server:
  port: 9006
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/XIDP-yanzemin/config.git
          search-paths: config
          default-label: main
          basedir: /Users/zemin.yan/Desktop/XIDP/config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
